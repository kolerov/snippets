set target_alias    "$env(ARC_TARGET_ALIAS)"
set target_triplet  "$env(ARC_TARGET_TRIPLET)"
set tmpdir          "tmp"

# For Newlib
set tool_version    "4.3.0"

switch $tool {
    gdb {
	source $srcdir/lib/append_gdb_boards_dir.exp
    }
}

set verbose 0
