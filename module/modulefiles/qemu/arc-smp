#%Module

proc ModulesHelp {} {
    puts stderr {QEMU for ARC targets (SMP version)}
    puts stderr {}
}

module-whatis {QEMU for ARC targets (SMP version)}

set home /tools/qemu-arc-smp
setenv QEMU_HOME $home
prepend-path PATH $home/bin
